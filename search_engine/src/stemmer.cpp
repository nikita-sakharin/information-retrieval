#include <search_engine/header.hpp>

inline constexpr int stemmer::stem() noexcept {
    ;
}

inline constexpr char *stemmer::stem(char * const, char * const) noexcept {
}

inline constexpr char *tolower(
    char * const dst,
    char * const src,
    const size_t
) noexcept {
    for () {
        if ()
    }
}
